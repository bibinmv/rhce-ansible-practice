---
- name: generate host file
  hosts: all
  tasks:
    - name: template the files to myhosts
      ansible.builtin.template:
        src: /home/devops/ansible/hosts.j2
        dest: /etc/myhosts
      when: "'dev' in group_names"
